document.addEventListener('DOMContentLoaded', function() {

    // Input formatter
    // ------------------------------

    // Date format
    $('[name="format-date"]').formatter({
        pattern: '{{99}}/{{99}}/{{9999}}'
    });
    
});